# Claude Cowork adapter

Use `AI_FUND_TALENT_CONTEXT.md` as the canonical non-sensitive operating context and `AGENTS.md` as the repository execution policy. Cowork remains a separate human-facing environment; it does not need to run as a Munder subprocess.
