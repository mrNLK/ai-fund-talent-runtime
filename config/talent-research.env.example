# Optional local override for Talent Researcher search tools.
# Copy to ~/.config/ai-fund-talent/research.env and fill in values.
# Never commit a filled copy. Hive files must not contain these keys.
#
# Sider Scholar uses the ChatGPT login on this Mac. It has no API key here.

EXA_API_KEY=
PARALLEL_API_KEY=
