# AI Fund Talent Runtime agent instructions

Mike is a non-technical operator. Own routine engineering and Git mechanics end to end, explain outcomes in plain language, and ask him only for consequential product, security, cost, external, destructive, or credential decisions.

Before acting:

1. Inspect the current state before editing.
2. Read `AI_FUND_TALENT_CONTEXT.md` for Talent work.
3. Make surgical changes and protect unrelated work.
4. Never expose or commit secrets, credentials, cookies, tokens, or `.env` files.

While working:

5. Use a focused branch, keep commits and pull requests clean, and resolve routine Git mechanics without asking Mike.
6. Run the relevant tests after changes. Never silently bypass a failing test.
7. Review the final diff and remove generated junk before committing.
8. Never deploy to production unless Mike explicitly authorizes it.
9. Never send candidate communications or modify candidate, ATS, CRM, calendar, or outreach records without Mike's explicit approval for the exact action.

`AI_FUND_TALENT_CONTEXT.md` is the canonical non-sensitive Talent policy. Tool-specific files should point to it rather than restating it.
